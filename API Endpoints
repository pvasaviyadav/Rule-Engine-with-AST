### API Endpoints:

- `POST /create_rule`: Create a rule from a rule string.
- `POST /combine_rules`: Combine multiple rules into a single AST.
- `POST /evaluate_rule`: Evaluate the combined AST against provided user data.
